/** Fester Loopback-Redirect für Supabase OAuth (Desktop). */
export const PROFILE_SYNC_OAUTH_PORT = 47842

export const PROFILE_SYNC_OAUTH_REDIRECT_URI = `http://127.0.0.1:${PROFILE_SYNC_OAUTH_PORT}`

export const PROFILE_SYNC_OAUTH_TIMEOUT_MS = 15 * 60 * 1000
